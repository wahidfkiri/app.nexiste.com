{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the local bundled bootstrap window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
